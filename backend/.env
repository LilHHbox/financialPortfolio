# 数据库连接配置（需与 docker-compose.yml 匹配）
DB_HOST=localhost     # Docker 中连接宿主机，或用 `db`（容器名）跨服务访问
DB_PORT=13306         # 宿主机映射端口
DB_USER=root           # 与 docker-compose 中 MYSQL_USER 一致
DB_PASS=hzy200804    # 与 docker-compose 中 MYSQL_PASSWORD 一致
DB_NAME=projectdb     # 与 docker-compose 中 MYSQL_DATABASE 一致

# 应用端口（根据实际需求调整）
PORT=3000
